fun encodeDecode() {
    val input = arrayOf("neet", "code", "love", "you","love")
    val encodedList = mutableListOf<String>()
    val encodedMap = mutableMapOf<String,ArrayDeque<String>>()
    val decodedList = mutableListOf<String>()

    // encode
    for (str in input) {
        val arr = IntArray(26)
        for (ch in str) {
            arr[ch - 'a']++
        }
       val key = arr.joinToString("#")
        encodedMap.getOrPut(key){ArrayDeque()}.addLast(str)
        encodedList.add(key)

    }

    // decode
    for (item in encodedList){
        val data = encodedMap[item]!!.removeFirst()
        decodedList.add(data)
    }
}


Time Complexity O(n * m)
Space Complexity O(n)
