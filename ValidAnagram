fun isValidAnagram():Boolean {
    val s = "racecar"
    val t = "carrce"
    val sMap = mutableMapOf<Char,Int>()
    val tMap = mutableMapOf<Char,Int>()

    if (s.length != t.length) {
        return false
    }
    for (i in s){
        sMap[i] = sMap.getOrDefault(i,0) + 1
    }

    for (i in t){
        tMap[i] = tMap.getOrDefault(i,0) + 1
    }

   return sMap == tMap
}

Time Complexity O(n)
Space Complexity O(n)
