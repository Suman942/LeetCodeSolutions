fun main(args: Array<String>) {
  val arr = intArrayOf(1, 2, 3, 4, 5)
  var i = 0 
  var j = arr.size - 1 
  while (i < j){ 
    val ith = arr[i] 
    val jth = arr[j] 
    arr[i] = jth
    arr[j] = ith
    i++
    j-- 
  }
  println(arr.toList())
}
