fun main(args: Array<String>) {
  val arr = intArrayOf(0,1,0,3,12)
  var i = 0

  for (j in arr.indices){
    if (arr[j] != 0){
      arr[i] = arr[j]
      i++
    }
  }
  while (i < arr.size){
    arr[i] = 0
    i++
  }

  println(arr.toList())

}
