fun productArrayExceptSelf() {
    val nums = arrayOf(1, 2, 3, 4)
    val n = nums.size
    val answer = IntArray(n)

    // 1) left pass: answer[i] will hold product of all elements to the left of i
    var leftProd = 1
    for (i in 0 until n) {
        answer[i] = leftProd
        leftProd *= nums[i]
    }
    // 2) right pass:  answer[i] will hold product of all elements to the rihgt of i
    var rightProd = 1
    for (i in n - 1 downTo 0) {
        answer[i] = answer[i] * rightProd
        rightProd *= nums[i]
    }

}

Time Complexity O(n)
Space Complexity O(n)
