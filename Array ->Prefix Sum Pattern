// Brute force approach

 val arr = intArrayOf(1,2,3,4,5)
  val queries = arrayOf(
    0 to 2,
    1 to 4,
    3 to 4
  )

  for ((L,R) in queries){
    var sum = 0
    for (i in L..R){
      sum +=arr[i]
    }
    println(sum)
  }


// optimal approach
